# api-control Specification

## Purpose
TBD - created by archiving change build-node-server. Update Purpose after archive.
## Requirements
### Requirement: REST API Endpoints
The system MUST provide REST API endpoints.

#### Scenario: Start Round via API
- **WHEN** POST /api/round/start is called
- **THEN** round becomes ACTIVE

